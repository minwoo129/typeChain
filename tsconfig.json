{
  "include": ["src"],
  "compilerOptions": {
    // 컴파일 자바스크립트 파일 저장 위치
    "outDir": "build",
    // 어떤 자바스크립트 버전으로 컴파일할지?
    "target": "ES6",
    // 목표로 하는 실행환경(자바스크립트 코드가 어디서 동작하는지 알려줌)
    "lib": ["ES6", "DOM"],
    // strict 모드 활성화 여부
    "strict": true
  }
}
